# checkStatusSuccess

*object* — defined in [thing.t](../by-file/thing.t.md) (line 202)


Success status object. Note that this is a single object, not a class - there's no distinct information per success indicator, so we only need this single success indicator for all uses.


**Superclass chain:** [CheckStatus](checkstatus.md) > `object` > **checkStatusSuccess**


## Properties


### `isSuccess` *(overridden)*

Defined in thing.t, line 203


## Inherited Properties


*From [CheckStatus](checkstatus.md):* [`msgParams`](checkstatus.md#msgParams), [`msgProp`](checkstatus.md#msgProp)
