# StackFrameRef

*class* — defined in [systype.h](../by-file/systype.h.md) (line 1316)


A StackFrameRef is an internal object used with StackFrameDesc. This type is used internally by the VM; user code can't create an instance of this class directly with 'new', and the class has no public methods.
